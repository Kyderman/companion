<%= simple_form_for(@teams_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :team_id %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
